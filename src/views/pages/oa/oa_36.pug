extends ../../includes/_layout_open.pug

block title
    title 理財密碼

block scss

block main

    .view-992
        #appOpen  
            .bg-marble
                .open-progress
                    .progress-line(style="width:75%")

                .finance-password
                    h1.main-title.text-center.pb-1 理財密碼
                    .text-center.tooltip 
                        span.txt.text-center.f14 理財密碼的用途?
                        span.info  
                            img.img(src=assets + "/images/open/icon/info@2x.png", alt="info") 
                        .tooltip-box.tex-left
                            p 理財密碼僅用於
                            p 1.大戶數位帳戶電話銀行服務驗證
                            p 2.大戶數位帳戶臨櫃提取服務驗證
                            p 3.若有其他帳戶需另外設定 

                    form.finance-form
                        .form-group.seperate-input.invalid-error
                            input.form-control(id="", type="password", maxlength="1")
                            input.form-control(id="", type="password", maxlength="1") 
                            input.form-control(id="", type="password", maxlength="1") 
                            input.form-control(id="", type="password", maxlength="1") 
                        p.f14.text-danger.text-center ２次密碼輸入不相同，請再次確認
                        p.text-center.mt-24.mb-0
                            a.text-underline.text-bold(href="") 重新設定理財密碼
                    .center.align-bottom
                        button.btn.btn-primary(type="submit") 下一步
                            

block js
